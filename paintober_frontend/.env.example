# Set this in Netlify → Site settings → Environment variables
# Leave empty (or omit) for local dev — the Vite proxy handles /api/* requests
VITE_API_BASE=https://66-228-41-226.ip.linodeusercontent.com
